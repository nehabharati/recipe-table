
1. It's always better to make fetch calls outside of component. 
2. So make a function outside the component where u make fetch call and return a promise from that function
3. Then use that function as a promise inside the component